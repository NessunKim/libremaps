table! {
    markers (id) {
        id -> Int4,
        name -> Varchar,
        latitude -> Float8,
        longitude -> Float8,
        zoom -> Int2,
        page_id -> Int4,
        page_name -> Varchar,
        page_revid -> Int4,
    }
}
