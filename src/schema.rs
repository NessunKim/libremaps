table! {
    markers (id) {
        id -> Integer,
        name -> Varchar,
        latitude -> Float,
        longitude -> Float,
        zoom -> Tinyint,
        page_id -> Integer,
        page_name -> Varchar,
        page_revid -> Integer,
    }
}
